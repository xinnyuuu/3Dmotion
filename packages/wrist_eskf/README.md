# wrist_eskf

Purpose:

```text
wrist IMU propagation + AprilTag pose correction -> smoothed wrist state
```

State:

```text
p_W, v_W, q_W, b_a, b_g
```

Implementation is intentionally empty in the skeleton.

